a <- b <- c <- 50   # promenne a,b,c prirazujeme hodnotu 50

if(a == b && b == c) {
	print("vsechna cisla si jsou rovna") # zde se program dostane
} else {
	print("alespon jedno z cisel je odlisny")
}
print("program vyhodnocen")
